# Next Steps

Highest-value follow-up work:

1. Replace `StubTranslationProvider` with a real local translation provider.
2. Replace `StubLookupProvider` with a real dictionary source.
3. Add better sentence segmentation for mixed English/Chinese streams.
4. Add explicit settings UI for language direction and provider config.
5. Decide whether to keep the control panel or move to tray-first behavior.
