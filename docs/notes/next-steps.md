# Next Steps

Highest-value follow-up work:

1. Validate release packaging on a clean Windows machine without system Python or development-only paths.
2. Decide whether the bundled `runtime` should stay mostly in normal Git, move more files to Git LFS, or move to a release artifact workflow.
3. Improve dictionary morphology fallback and support richer entry shapes.
4. Add better sentence segmentation for mixed English/Chinese streams.
5. Improve settings UX for language direction and provider-specific configuration.
6. Decide whether to keep the control panel or move to tray-first behavior.
